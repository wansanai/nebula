[package]
name = "app-core"
version.workspace = true
description = "Framework-agnostic Nebula app logic over the provider registry (used by the Tauri shell)"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
cloud-core = { path = "../cloud-core" }
nebula-provider = { path = "../nebula-provider" }
nebula-image = { path = "../nebula-image" }
nebula-matting = { path = "../nebula-matting" }
nebula-pdf = { path = "../nebula-pdf" }
provider-aliyun = { path = "../providers/provider-aliyun" }
provider-tencent = { path = "../providers/provider-tencent" }
provider-huawei = { path = "../providers/provider-huawei" }
provider-qiniu = { path = "../providers/provider-qiniu" }
provider-aws = { path = "../providers/provider-aws" }
provider-r2 = { path = "../providers/provider-r2" }
provider-minio = { path = "../providers/provider-minio" }
provider-b2 = { path = "../providers/provider-b2" }
provider-wasabi = { path = "../providers/provider-wasabi" }
provider-do-spaces = { path = "../providers/provider-do-spaces" }
provider-scaleway = { path = "../providers/provider-scaleway" }
provider-us3 = { path = "../providers/provider-us3" }
provider-jdcloud = { path = "../providers/provider-jdcloud" }
provider-upyun = { path = "../providers/provider-upyun" }
bytes = { workspace = true }
futures = { workspace = true }
async-trait = "0.1"
# 断点续传上传要按偏移读本地文件分片;rt 提供 spawn_blocking(图片解码放阻塞线程池)。
tokio = { workspace = true, features = ["fs", "io-util", "rt"] }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
rusqlite = { version = "0.32", features = ["bundled"] }
# 插件下载(AI 抠图的模型 + ONNX Runtime 运行时库)。
reqwest = { workspace = true }
flate2 = "1"
tar = "0.4"
zip = { version = "2", default-features = false, features = ["deflate"] }
# 各平台原生后端:macOS Keychain / Windows Credential / Linux 内核 keyutils
# (linux-native 为纯 Rust,不依赖 libdbus / openssl 等系统库,CI 可直接编译)。
keyring = { version = "3", default-features = false, features = [
    "apple-native",
    "windows-native",
    "linux-native",
] }

[dev-dependencies]
# test-util 提供虚拟时钟(start_paused),用于确定性地测限速。
tokio = { workspace = true, features = ["test-util"] }
